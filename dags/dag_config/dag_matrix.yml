- name: foo
  vars:
    one: two
  scheduler:
    enable: true
    interval: "0 12 * * 1,3,5"
  tasks:
  - foo.yml
- name: bar
  vars:
    three: four
  scheduler:
    enable: true
    interval: "0 12 * * 1,3,5"
  tasks: 
  - bar.yml
